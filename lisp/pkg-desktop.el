;;; -*- lexical-binding: t; -*-

;; (use-package desktop
;;   :disabled
;;   :ensure nil
;;   :custom
;;   (desktop-dirname user-emacs-directory)
;;   (desktop-restore-frames t)
;;   ;; バッファ & グローバル変数は何も保存しない
;;   (desktop-buffers-not-to-save ".*")
;;   (desktop-globals-to-save nil)
;;   :init
;;   (desktop-save-mode 1)
;; )

(provide 'pkg-desktop)
